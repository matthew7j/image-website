extends layout

block content
  div(class='image_container') 
    div(class='row')
      div(class='col s12')
        div(class='card')
          div(class='card-image')
            img(src=image.signedUrl)